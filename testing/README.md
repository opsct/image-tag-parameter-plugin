Easy deploy ssl regitry with traefik
user creds test/test

- just install docker and docker-compose
- pick a domainname (replace yourdomain in the yaml file)
- point dns name traefik and registry to the server

